<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.reservation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ReserActivity" />
        <activity android:name=".NorthActivity" />
        <activity android:name=".CentralActivity" />
        <activity android:name=".SouthActivity" />
        <activity android:name=".EastActivity" />
        <activity android:name=".Top10Activity" />
        <activity android:name=".TopinfoActivity" />
        <activity android:name=".Top2Activity" />
        <activity android:name=".Top2infoActivity" />
        <activity android:name=".DistanceActivity" />
        <activity android:name=".RecordActivity" />
        <activity android:name=".MyaddresssActivity"></activity>
    </application>

</manifest>